name: Windows Build

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-2022

    steps:
      - uses: actions/checkout@v4

      - name: Mostra informazioni di sistema
        run: |
          systeminfo
          ver

      - name: Esegui un comando
        run: echo Ciao da GitHub Actions!
